// Route skeleton for Fase 1 — preserves the /devlog URL shape from the Vite
// app (src/pages/DevlogList.jsx) so it isn't lost during the routing
// migration. Real content + styling arrives in Fase 4.
export default function DevlogListPage() {
  return (
    <main style={{ padding: '4rem', textAlign: 'center' }}>
      <h1>Devlog</h1>
      <p>Placeholder — devlog list content migrates in Fase 4.</p>
    </main>
  )
}
